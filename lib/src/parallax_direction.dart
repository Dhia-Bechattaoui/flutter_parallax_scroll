/// Defines the direction of parallax scrolling effect
enum ParallaxDirection {
  /// Parallax effect moves in the same direction as scroll
  forward,

  /// Parallax effect moves in the opposite direction as scroll
  reverse,

  /// Parallax effect moves horizontally while scrolling vertically
  horizontal,

  /// Parallax effect moves vertically while scrolling horizontally
  vertical,
}
